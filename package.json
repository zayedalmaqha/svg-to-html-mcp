{
  "name": "svg-to-html-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "A Claude MCP tool to convert artifact TypeScript code to HTML pages",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "install-tool": "node install.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.0.0"
  },
  "bin": {
    "svg-to-html-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}